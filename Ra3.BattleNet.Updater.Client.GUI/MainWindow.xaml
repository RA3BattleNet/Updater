<Window x:Class="Ra3.BattleNet.Updater.Client.GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="增量更新" Height="350" Width="500" WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtStatus" Grid.Row="0" Text="正在检查更新..." FontSize="14" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,5">
            <ProgressBar x:Name="ProgressBarUpdate" Height="22" Minimum="0" Maximum="100"/>
            <TextBlock x:Name="TxtFile" Text="" Margin="0,3,0,0" Foreground="Gray"/>
        </StackPanel>

        <TextBox x:Name="TxtLog" Grid.Row="2" IsReadOnly="True" VerticalScrollBarVisibility="Auto"
                 Background="#1E1E1E" Foreground="#D4D4D4" FontFamily="Consolas"
                 TextWrapping="Wrap" AcceptsReturn="True" Margin="0,5,0,0"/>
    </Grid>
</Window>
